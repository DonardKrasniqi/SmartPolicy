# SmartPolicy

The web app and API code live in the **`projekti/`** directory. For [Vercel](https://vercel.com) deployment, set the project **Root Directory** to **`projekti`** and follow **Deploy on Vercel** in [projekti/README.md](projekti/README.md).